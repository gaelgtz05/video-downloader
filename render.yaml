services:
  - type: web
    name: video-downloader # O el nombre que le hayas puesto en Render
    env: python
    plan: starter
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 300"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
